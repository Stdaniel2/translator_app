# English–Igbo Translator API
This project is a FastAPI-based deployment of a MarianMT Transformer model for English-to-Igbo translation.

## Setup
pip install -r requirements.txt

## Run
uvicorn app.main:app --reload
